<%= simple_form_for([:admin, @about], :html => {:class => 'form-horizontal', :multipart => true}) do |f| %>
  <%= f.input :about_text, :input_html => {:class => "tinymce", :rows => 15, :cols => 120} %>
  <%= tinymce %>
  <%= f.button :submit %>
<% end %>
