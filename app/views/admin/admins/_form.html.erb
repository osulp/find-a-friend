<div class="container">
  <div class="row">
    <div class="col-xs-8">
      <%= simple_form_for([:admin, @admin], :html => {:class => "form-horizontal"}) do |f| %>
        <%= f.input :onid %>
        <%= f.button :submit %>
      <% end %>
    </div>
  </div>
</div>

